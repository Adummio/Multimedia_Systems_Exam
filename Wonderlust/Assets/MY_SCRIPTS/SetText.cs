using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SetText : MonoBehaviour
{
    /*
    public Text prova;
    // Start is called before the first frame update
    public void Start()
    {
        prova.enabled = false;
    }

    // Update is called once per frame
    void Update()
    {
        if (ActivateOnTouch.activateme == true)
        {
            //GameObject.Find("CanvasOptions/M/MenuParent").GetComponent<MainMenu>().SetID(nomePOI);
            prova.text = ActivateOnTouch.nomePOI;
            prova.enabled = true;

        }

    }*/
}
